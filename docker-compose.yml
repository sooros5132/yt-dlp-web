version: "3"

services:
  yt-dlp-web:
    container_name: yt-dlp-web
    build:
      context: ./
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - ~/Desktop/downloads:/downloads
    ports:
      - 3000:3000
    restart: unless-stopped
